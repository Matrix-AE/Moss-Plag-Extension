# Manual extension test files

These are synthetic Python fixtures for testing the extension UI. `submission-a/word_count.py` and `submission-b/word_count.py` intentionally share several functions and lines, while B also contains unique code.

Load the two `word_count.py` files as the two separate submissions. Do not upload real student or client code while testing.
